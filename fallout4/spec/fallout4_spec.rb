require "spec_helper"

RSpec.describe Fallout4 do
  it "has a version number" do
    expect(Fallout4::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
